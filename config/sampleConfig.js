// SAMPLE LINODE CONFIG
// Duplicate this file as 'linodeConfig.js' and add your Linode API Key to use
// Note: you will only be able to use API methods for which your key has permissions

'use strict';

const linodeConfig = {
	apiKey: '[YOUR_API_KEY]',
	baseUrl: 'https://api.linode.com/v4/'
}

module.exports = linodeConfig;
